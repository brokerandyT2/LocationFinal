<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Location.Photography.Maui.Views.Premium.FieldOfView"
             Title="Field of View Preview"
             BackgroundColor="Black">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header with camera selection -->
        <Frame Grid.Row="0" 
               BackgroundColor="#80000000" 
               Margin="20,40,20,10" 
               Padding="20"
               CornerRadius="10">
            <StackLayout>
                <Label Text="Field of View Preview" 
                       FontSize="22" 
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center" />

                <Label Text="Capture a photo to see field of view comparison with different camera lenses"
                       FontSize="14"
                       TextColor="White"
                       HorizontalTextAlignment="Center"
                       Margin="0,10,0,0" />
            </StackLayout>
        </Frame>

        <!-- Image preview with overlay -->
        <Grid Grid.Row="1" Margin="10">
            <!-- Captured Image Display -->
            <Image x:Name="CapturedImage" 
                   Aspect="AspectFill"
                   VerticalOptions="FillAndExpand"
                   HorizontalOptions="FillAndExpand"
                   BackgroundColor="#333333" />

            <!-- Placeholder when no image -->
            <StackLayout x:Name="PlaceholderStack"
                        VerticalOptions="Center"
                        HorizontalOptions="Center"
                        IsVisible="True">
                <Label Text="📸" 
                       FontSize="60" 
                       TextColor="Gray"
                       HorizontalOptions="Center" />
                <Label Text="Tap 'Take Photo' to capture an image"
                       FontSize="16"
                       TextColor="Gray"
                       HorizontalTextAlignment="Center" />
            </StackLayout>

            <!-- FOV Overlay -->
            <GraphicsView x:Name="OverlayGraphicsView"
                         VerticalOptions="FillAndExpand"
                         HorizontalOptions="FillAndExpand"
                         BackgroundColor="Transparent"
                         IsVisible="False" />
        </Grid>

        <!-- Controls -->
        <Frame Grid.Row="2" 
               BackgroundColor="#80000000" 
               Margin="20,10,20,40" 
               Padding="20"
               CornerRadius="10">
            <StackLayout Spacing="15">

                <!-- Capture Button -->
                <Button x:Name="CaptureButton"
                        Text="📸 Take Photo"
                        FontSize="16"
                        FontAttributes="Bold"
                        BackgroundColor="#007ACC"
                        TextColor="White"
                        HeightRequest="50"
                        CornerRadius="25"
                        Clicked="OnCaptureButtonClicked" />

                <!-- Camera Selection -->
                <Label Text="Camera Lens:" TextColor="White" FontAttributes="Bold" />
                <Picker x:Name="CameraPicker" 
                        Title="Select Camera Lens"
                        TextColor="White"
                        TitleColor="Gray"
                        SelectedIndexChanged="OnCameraPickerSelectedIndexChanged">
                    <Picker.Items>
                        <x:String>Phone Camera</x:String>
                        <x:String>Custom Focal Length</x:String>
                    </Picker.Items>
                </Picker>

                <!-- Focal Length Input -->
                <StackLayout x:Name="FocalLengthStack" 
                            Orientation="Horizontal" 
                            Spacing="10"
                            IsVisible="False">
                    <Label Text="Focal Length:" 
                           TextColor="White" 
                           VerticalOptions="Center" />
                    <Entry x:Name="FocalLengthEntry" 
                           Placeholder="50" 
                           Keyboard="Numeric"
                           TextColor="White"
                           PlaceholderColor="Gray"
                           BackgroundColor="#40FFFFFF"
                           WidthRequest="80"
                           TextChanged="OnFocalLengthChanged" />
                    <Label Text="mm" 
                           TextColor="White" 
                           VerticalOptions="Center" />
                </StackLayout>

                <!-- FOV Information -->
                <StackLayout x:Name="FOVInfoStack" Spacing="5">
                    <Label x:Name="PhoneFOVLabel" 
                           Text="Phone FOV: Calculating..." 
                           TextColor="Cyan" 
                           FontSize="14" />
                    <Label x:Name="CameraFOVLabel" 
                           Text="Selected FOV: --" 
                           TextColor="Orange" 
                           FontSize="14" />
                    <Label x:Name="ComparisonLabel" 
                           Text="Comparison: --" 
                           TextColor="Yellow" 
                           FontSize="14" />
                </StackLayout>

                <!-- Help Text -->
                <Label Text="The cyan box shows your phone's field of view. The orange box shows the selected camera's field of view."
                       FontSize="12"
                       TextColor="#CCCCCC"
                       HorizontalTextAlignment="Center" />
            </StackLayout>
        </Frame>

        <!-- Processing Overlay -->
        <Grid x:Name="ProcessingOverlay"
              Grid.RowSpan="3"
              BackgroundColor="#CC000000"
              IsVisible="False">
            <StackLayout VerticalOptions="Center" 
                        HorizontalOptions="Center"
                        Spacing="15">
                <ActivityIndicator IsRunning="True" 
                                  Color="White" 
                                  HeightRequest="50" 
                                  WidthRequest="50"/>
                <Label Text="Processing image..." 
                       TextColor="White" 
                       FontSize="16"/>
            </StackLayout>
        </Grid>
    </Grid>
</ContentPage>